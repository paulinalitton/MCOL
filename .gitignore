# macOS Finder metadata
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Local env
.env
.env.local

# Scratch / WIP authoring directory — copy library + injector for the
# in-progress SEO localization pass. Not for commit; the diffs land in
# the HTML pages themselves.
tmp_content/
